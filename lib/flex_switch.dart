/// Flex Switch — a fast, accessible, multi‑option segmented control for Flutter.
///
/// Example:
/// ```dart
/// final widget = FlexSwitch.boolean(
///   value: enabled,
///   onChanged: (v) => setState(() => enabled = v),
///   trueLabel: 'On',
///   falseLabel: 'Off',
/// );
/// ```
library;

export 'src/flex_switch.dart';
export 'src/flex_switch_style.dart';
